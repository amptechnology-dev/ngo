services:
  app:
    build: .
    container_name: bhagabanpur
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3021"
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: amp_portal_backend_app-network
